import 'package:flutter/material.dart';

class BookmarkView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('Esta es la vista de Marcadores'),
    );
  }
}